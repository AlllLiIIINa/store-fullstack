export class Item {
    id: number;
    name: string;
    description: string;
    price: string;

    constructor(id: number, name: string, description: string, price: string) {
      this.id = id;
      this.name = name;
      this.description = description;
      this.price = price;
    }
  }
  